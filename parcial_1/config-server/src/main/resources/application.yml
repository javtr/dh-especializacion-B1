server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/javtr/spring-cloud-example
          default-label: master
          search-paths: caja